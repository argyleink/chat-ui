chat-ui.loading(grid='rows' vertically-distributed='equal')
  chat-titlebar(grid vertically-aligned='center' horizontally-distributed='between')
    //- span ←
    h5 Chat UI
    chat-avatar: img(src='')

  .chat-ui_scrollview: main(grid='rows' horizontally-aligned='right')
    chat-cluster(grid vertically-aligned='bottom')
      chat-avatar: img(src='')
      section(grid='rows')
        h3 ${bot_name}
        chat-message Hi!
        chat-message I'm a bot 😉
        chat-message Hopefully you find me useful some how
    chat-cluster(grid vertically-aligned='bottom')
      chat-avatar: img(src='')
      section(grid='rows')
        h3 ${bot_name}
        chat-message Hi!
        chat-message I'm a bot 😉
        chat-message Hopefully you find me useful some how
    chat-cluster(grid vertically-aligned='bottom')
      chat-avatar: img(src='')
      section(grid='rows')
        h3 ${bot_name}
        chat-message Hi!
        chat-message I'm a bot 😉
        chat-message Hopefully you find me useful some how
    chat-cluster(mine grid vertically-aligned='bottom')
      chat-avatar: img(src='')
      section(grid='rows')
        h3 Not Bot 
        chat-message I'm <i>NOT</i> a bot
        chat-message 😉
        chat-message what kind of things can you do?
    chat-cluster(indeterminate grid vertically-aligned='bottom')
      chat-avatar: img(src='')
      section(grid='rows')
        h3 ${bot_name}
        chat-message ...

    chat-authoring